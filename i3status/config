general {
	output_format = "i3bar"
	colors = true
	interval = 10
}

order += "wireless _first_"
order += "ethernet _first_"
order += "cpu_usage"
order += "load"
order += "time"
order += "volume master"

wireless _first_ {
	format_up = "%bitrate @%essid"
	format_down = ""
}

ethernet _first_ {
	format_up = ""
	format_down = " "
}

cpu_usage {
	format = " %usage"
}

load {
	format = " %1min %5min %15min"
	max_threshold = 4
}

time {
	format = " %Y-%m-%d %H:%M"
}

volume master {
	format = "%volume"
	format_muted = " %volume"
	device = "default"
	mixer = "Master"
	mixer_idx = 0
}
