# ß                         
general {
	output_format = "i3bar"
	colors = true
	interval = 5
}

order += "wireless _first_"
order += "ethernet _first_"
order += "battery 0"
order += "cpu_temperature 0"
order += "cpu_usage"
order += "load"
order += "time"
order += "volume master"

wireless _first_ {
	format_up = "%bitrate @%essid"
	format_down = ""
}

ethernet _first_ {
	format_up = ""
	format_down = " "
}

battery 0 {
	format = "%status %percentage %remaining (%consumption)"
	status_chr = ""
	status_bat = ""
	status_full = ""
	last_full_capacity = false
	integer_battery_capacity = true
	low_threshold = 30
	threshold_type = time
}

cpu_temperature 0 {
	format = " %degrees°C"
	max_threshold = 86
	path = "/sys/devices/platform/applesmc.768/temp12_input"
}

cpu_usage {
	format = " %usage"
}

load {
	format = " %1min %5min %15min"
	max_threshold = 4
}

time {
	format = " %Y-%m-%d %H:%M"
}

volume master {
	format = "%volume"
	format_muted = " %volume"
	device = "default"
	mixer = "Master"
	mixer_idx = 0
}
